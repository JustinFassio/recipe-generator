import { CuisineStaplesData } from '../cuisine-staples';

export const additionalAsianCuisines: Record<string, CuisineStaplesData> = {
  // Note: Korean, Japanese, Vietnamese are already in the main file
  // This file is empty for now - all Asian cuisines are already in the main file
};
