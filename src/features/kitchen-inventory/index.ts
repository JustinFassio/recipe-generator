// Kitchen Inventory feature entrypoint (barrel)
// Expose page wrapper and helpers while reusing existing hooks/components

export { KitchenInventoryPage } from './page-wrapper';
export { resolveCategoryForIngredient } from './utils/resolve-category';
